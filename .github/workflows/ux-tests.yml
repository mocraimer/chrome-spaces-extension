name: UX Test Suite

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run nightly at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      test_suite:
        description: 'Test suite to run'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - smoke
          - user-journeys
          - interaction-flows
          - accessibility
          - error-ux
          - performance
          - bdd

jobs:
  # Job 1: Unit Tests (Fast, Always Run)
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test -- --coverage --maxWorkers=50%

      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/lcov.info
          flags: unittests

  # Job 2: Build Extension
  build:
    name: Build Extension
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build extension
        run: npm run build

      - name: Verify build artifacts
        run: |
          test -d build || (echo "Build directory not found" && exit 1)
          test -f build/manifest.json || (echo "Manifest not found" && exit 1)
          test -f build/background.js || (echo "Background script not found" && exit 1)

      - name: Upload build artifact
        uses: actions/upload-artifact@v3
        with:
          name: extension-build
          path: build/
          retention-days: 7

  # Job 3: Smoke Tests (Critical User Paths)
  smoke-tests:
    name: Smoke Tests (BDD)
    runs-on: ubuntu-latest
    needs: [build]
    timeout-minutes: 15
    if: always()

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: extension-build
          path: build/

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run BDD smoke tests
        run: npm run test:bdd:smoke
        continue-on-error: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: smoke-test-results
          path: |
            cucumber-report.html
            test-results/
          retention-days: 30

  # Job 4: User Journey Tests
  user-journey-tests:
    name: User Journey Tests
    runs-on: ubuntu-latest
    needs: [build, smoke-tests]
    timeout-minutes: 30
    if: github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'user-journeys' || github.event.inputs.test_suite == ''

    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: extension-build
          path: build/

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run user journey tests (shard ${{ matrix.shard }}/2)
        run: npm run test:e2e -- e2e-tests/user-journeys/ --shard=${{ matrix.shard }}/2
        continue-on-error: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: user-journey-results-${{ matrix.shard }}
          path: test-results/
          retention-days: 30

  # Job 5: Interaction Flow Tests
  interaction-flow-tests:
    name: Interaction Flow Tests
    runs-on: ubuntu-latest
    needs: [build, smoke-tests]
    timeout-minutes: 25
    if: github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'interaction-flows' || github.event.inputs.test_suite == ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: extension-build
          path: build/

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run interaction flow tests
        run: npm run test:e2e -- e2e-tests/interaction-flows/
        continue-on-error: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: interaction-flow-results
          path: test-results/
          retention-days: 30

  # Job 6: Accessibility Tests
  accessibility-tests:
    name: Accessibility Tests (WCAG 2.1 AA)
    runs-on: ubuntu-latest
    needs: [build]
    timeout-minutes: 30
    if: github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'accessibility' || github.event.inputs.test_suite == ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: extension-build
          path: build/

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run accessibility tests
        run: npm run test:e2e -- e2e-tests/accessibility-ux/
        continue-on-error: true

      - name: Generate accessibility report
        if: always()
        run: |
          echo "# Accessibility Test Results" > accessibility-report.md
          echo "" >> accessibility-report.md
          echo "Test run: $(date)" >> accessibility-report.md
          echo "" >> accessibility-report.md
          echo "See test-results/ for detailed violation reports" >> accessibility-report.md

      - name: Upload accessibility results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: accessibility-results
          path: |
            test-results/
            accessibility-report.md
          retention-days: 90  # Keep longer for compliance

      - name: Comment PR with accessibility violations
        if: github.event_name == 'pull_request' && failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '⚠️ **Accessibility tests failed!**\n\nWCAG 2.1 violations detected. Please review the [test results](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}).'
            })

  # Job 7: Error UX Tests
  error-ux-tests:
    name: Error UX Tests
    runs-on: ubuntu-latest
    needs: [build, smoke-tests]
    timeout-minutes: 20
    if: github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'error-ux' || github.event.inputs.test_suite == ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: extension-build
          path: build/

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run error UX tests
        run: npm run test:e2e -- e2e-tests/error-ux/
        continue-on-error: true

      - name: Upload error test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: error-ux-results
          path: test-results/
          retention-days: 30

  # Job 8: Performance Tests
  performance-tests:
    name: User-Perceived Performance Tests
    runs-on: ubuntu-latest
    needs: [build, smoke-tests]
    timeout-minutes: 25
    if: github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'performance' || github.event.inputs.test_suite == ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: extension-build
          path: build/

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run performance tests
        run: npm run test:e2e -- e2e-tests/user-perceived-performance/
        continue-on-error: true

      - name: Upload performance results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: performance-results
          path: test-results/
          retention-days: 90  # Keep longer for trend analysis

  # Job 9: BDD Feature Tests
  bdd-tests:
    name: BDD Feature Tests
    runs-on: ubuntu-latest
    needs: [build]
    timeout-minutes: 20
    if: github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'bdd' || github.event.inputs.test_suite == ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: extension-build
          path: build/

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run BDD tests
        run: npm run test:bdd:report
        continue-on-error: true

      - name: Upload BDD report
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: bdd-report
          path: cucumber-report.html
          retention-days: 30

  # Job 10: Generate Comprehensive Report
  generate-report:
    name: Generate Test Report
    runs-on: ubuntu-latest
    needs: [unit-tests, smoke-tests, user-journey-tests, interaction-flow-tests, accessibility-tests, error-ux-tests, performance-tests, bdd-tests]
    if: always()
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v3

      - name: Generate comprehensive report
        run: |
          cat > test-summary.md << 'EOF'
          # UX Test Suite Report

          **Run Date**: $(date)
          **Commit**: ${{ github.sha }}
          **Branch**: ${{ github.ref_name }}

          ## Summary

          | Test Suite | Status |
          |------------|--------|
          | Unit Tests | ${{ needs.unit-tests.result }} |
          | Smoke Tests | ${{ needs.smoke-tests.result }} |
          | User Journey Tests | ${{ needs.user-journey-tests.result }} |
          | Interaction Flow Tests | ${{ needs.interaction-flow-tests.result }} |
          | Accessibility Tests | ${{ needs.accessibility-tests.result }} |
          | Error UX Tests | ${{ needs.error-ux-tests.result }} |
          | Performance Tests | ${{ needs.performance-tests.result }} |
          | BDD Tests | ${{ needs.bdd-tests.result }} |

          ## Artifacts

          - [Smoke Test Results](smoke-test-results/)
          - [User Journey Results](user-journey-results-1/) | [Shard 2](user-journey-results-2/)
          - [Interaction Flow Results](interaction-flow-results/)
          - [Accessibility Results](accessibility-results/)
          - [Error UX Results](error-ux-results/)
          - [Performance Results](performance-results/)
          - [BDD Report](bdd-report/cucumber-report.html)

          ## Next Steps

          1. Review failed tests
          2. Check accessibility violations
          3. Analyze performance regressions
          4. Update test documentation
          EOF

      - name: Upload comprehensive report
        uses: actions/upload-artifact@v3
        with:
          name: test-summary
          path: test-summary.md
          retention-days: 90

      - name: Comment PR with summary
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const summary = `## 🧪 UX Test Suite Results

            | Test Suite | Status |
            |------------|--------|
            | Unit Tests | ${{ needs.unit-tests.result }} |
            | Smoke Tests | ${{ needs.smoke-tests.result }} |
            | User Journeys | ${{ needs.user-journey-tests.result }} |
            | Interaction Flows | ${{ needs.interaction-flow-tests.result }} |
            | Accessibility | ${{ needs.accessibility-tests.result }} |
            | Error UX | ${{ needs.error-ux-tests.result }} |
            | Performance | ${{ needs.performance-tests.result }} |
            | BDD Tests | ${{ needs.bdd-tests.result }} |

            [View detailed results](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: summary
            })